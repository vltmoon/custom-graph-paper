import React from 'react';
import PropTypes from 'prop-types';

export default function Header() {
    return (
		<div style={header}>
            gridesigner
        </div>    	
	)
}

const header = {
    fontSize: 30,
    alignSelf: 'center'
}